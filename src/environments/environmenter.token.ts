// environmenter.token.ts
import { InjectionToken } from '@angular/core';

// A última string é só uma descrição para ajudar a debuggar
export const ENVIRONMENTER = new InjectionToken('Environmenter');